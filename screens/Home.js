import React from 'react';
import { StyleSheet, Text, View, Button } from "react-native";

export default function home({navigation}) { 
   return( <View>
       <Text>trying to keep up</Text>
   </View>
   )
}